MONGODB_URI="mongodb+srv://<db_user>:<db_password>@<cluster>.mongodb.net/?retryWrites=true&w=majority"
# Replace <db_user>, <db_password>, and <cluster> with your MongoDB Atlas values.
# Save a local copy as `.env` (do NOT commit `.env` to the repository).
